import React from "react";
import { Text, Screen } from "@/components";

const Home: React.FC = () => {
  return (
    <Screen>
      <Text type="Title">Hello World</Text>
    </Screen>
  );
};

export default Home;
